<p id="notice"><%= notice %></p>

<p>
  <b>Event:</b>
  <%= link_to @party.event.name, @party.event %>
</p>

<p>
  <b>Name:</b>
  <%= @party.name %>
</p>

<p>
  <b>Guests</b>
  <ul>
    <% @party.guests.each do |guest| %>
      <li><%= link_to guest.name, guest %></li>
    <% end %>
  </ul>
</p>


<%= link_to 'Edit', edit_party_path(@party) %> |
<%= link_to 'Back', parties_path %>
